/**
 * DEPRECATED — replaced by db-push.mjs (Supabase Postgres).
 *
 * SQLite schema is no longer used. Run `npm run db:push` instead.
 */
console.log("migrate.mjs is obsolete. The database is now Supabase Postgres.");
console.log("Run:  npm run db:push   to push the full schema to Supabase.");
process.exit(0);
