/**
 * DEPRECATED — this script used better-sqlite3 against the local SQLite DB.
 * The database is now Supabase Postgres; there is no local acc-jobs.db.
 *
 * To seed schedule events for testing, create them via the /jobs UI or
 * POST directly to /api/schedule-events from the browser console.
 */
console.log("seed-schedule.mjs is obsolete. The database is now Supabase Postgres.");
console.log("Create schedule events via the /jobs UI or POST to /api/schedule-events.");
process.exit(0);
