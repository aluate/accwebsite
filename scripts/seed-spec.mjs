/**
 * DEPRECATED — this script used better-sqlite3 against the local SQLite DB.
 * The database is now Supabase Postgres; there is no local acc-jobs.db.
 *
 * To seed a test job + spec, create one via the /jobs/new UI, or use the
 * Supabase table editor to insert rows directly.
 */
console.log("seed-spec.mjs is obsolete. The database is now Supabase Postgres.");
console.log("Create test specs via the /jobs/new UI or the Supabase table editor.");
process.exit(0);
